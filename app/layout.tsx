import './globals.css';
import type { Metadata } from 'next';
import { Inter } from 'next/font/google';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'EverythingAI Foundation - Empowering Entrepreneurs for Impact',
  description: 'Join a global community of visionary founders building AI solutions that create meaningful impact. Access mentorship, funding, and resources to accelerate your startup.',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
